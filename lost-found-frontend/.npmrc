# Ensure dependencies are locked with exact versions during installation
save-exact=true

# Enforce Node engine version check during installation
engine-strict=true

# Keep the console cleaner by suppressing irrelevant warnings
loglevel=warn
